<!doctype html>
<html lang="is">
  <head>
    <meta charset="utf-8">
    <title><%= view.title %></title>
    <% if (view.call == 'admin') { %>
      <link rel="stylesheet" href="../styles.css">
    <% } else { %> 
      <link rel="stylesheet" href="styles.css">
    <% } %>
  </head>
  <body>
    <main>
